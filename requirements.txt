# --- Core API + serving ---
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.26                       # TestClient transport (FastAPI 0.99+)
prometheus-fastapi-instrumentator>=7.0  # /metrics for Prometheus

# --- Data + ML ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
tensorflow>=2.15                  # loads the .keras BiLSTM/GRU/CNN artifacts
joblib>=1.3                       # legacy compatibility for archived .pkl

# --- Notebook-as-pipeline (train_pipeline.py wraps Lstm_PM_py.ipynb) ---
nbformat>=5.10
nbclient>=0.10
nbconvert>=7.16

# --- Plotting (used by EDA + LSTM diagnostic cells) ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# --- MLOps ---
mlflow>=2.10
evidently>=0.7                    # data drift (data_drift.py)
apache-airflow>=2.8               # weekly retrain DAG (retrain_dag.py)

# --- Testing ---
pytest>=8.0
