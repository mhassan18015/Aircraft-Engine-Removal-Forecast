# Streamlit Cloud deployment dependencies.
# Slim set — only what streamlit_app.py + api_inference.py import at runtime.
# For local training / MLOps / tests, install requirements-dev.txt instead.

streamlit>=1.36

# --- ML inference ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
tensorflow-cpu>=2.15            # Streamlit Cloud has no GPU; CPU build is smaller
joblib>=1.3                     # loads degradation_rf.pkl

# --- Pulled in transitively by api_inference.py (FlightRow / PredictionRequest) ---
fastapi>=0.109
pydantic>=2.6
